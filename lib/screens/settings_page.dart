import 'package:flutter/material.dart';

/// 設定ページ（非推奨）
class SettingsPage extends StatelessWidget {
  /// コンストラクタ
  const SettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Deprecated')));
  }
}
