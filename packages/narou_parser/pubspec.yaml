name: narou_parser
description: なろう小説パーサー
version: 0.1.0
publish_to: "none" # 内部パッケージ（path 依存を持つ）のため公開しない

environment:
  sdk: ^3.8.1

resolution: workspace

dependencies:
  freezed_annotation:
  html: ^0.15.7
  json_annotation:
  meta: ^1.12.0
  novel_parser_core:
    path: ../novel_parser_core

dev_dependencies:
  build_runner:
  freezed:
  json_serializable: ^6.11.2
  test:
  very_good_analysis: ^10.3.0
