name: wakachigaki
description: 軽量日本語分かち書きライブラリ (yuhsak/wakachigaki ベースのクリーンルーム実装)
version: 0.1.0
environment:
  sdk: ^3.8.1
resolution: workspace
dependencies:
  # 純 Dart、Flutter 非依存
dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
