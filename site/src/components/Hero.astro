---
import { Download, ArrowRight } from '@lucide/astro';
---

<section class="hero-section">
  <!-- Dynamic Background Effects -->
  <div class="bg-effects">
    <div class="blob blob-1"></div>
    <div class="blob blob-2"></div>
  </div>

  <div class="hero-content">
    <div class="content-wrapper">
      <div class="badge">
        <span class="ping-wrapper">
          <span class="ping-dot"></span>
          <span class="dot"></span>
        </span>
        Web小説読書体験の再定義
      </div>
      
      <h1 class="headline">
        Novelty<br/>
        <span class="sub-headline">モダンでシンプルな</span>
        <span class="gradient-text">
          小説ビューアー
        </span>
      </h1>
      
      <p class="description">
        「小説家になろう」を、もっと自由に、美しく。<br class="hide-mobile"/>
        広告なし、オフライン対応。物語に没入するための<br class="hide-mobile"/>
        Flutter製クロスプラットフォーム・クライアント。
      </p>
      
      <div class="cta-group">
        <a 
          href="https://github.com/L4Ph/Novelty/releases/latest" 
          class="btn btn-primary"
        >
          <Download class="icon" />
          今すぐダウンロード
        </a>
        <a 
          href="#features" 
          class="btn btn-ghost"
        >
          機能を見る
          <ArrowRight class="icon-sm" />
        </a>
      </div>
    </div>
  </div>

  <!-- Abstract Bottom Decoration -->
  <div class="bottom-decoration"></div>
</section>

<style>
  .hero-section {
    position: relative;
    overflow: hidden;
    min-height: 80vh;
    padding-top: 5rem;
    padding-bottom: 5rem;
    background-color: var(--color-base-100);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* Background Effects */
  .bg-effects {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
  }

  .blob {
    position: absolute;
    border-radius: 50%;
    filter: blur(120px);
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  .blob-1 {
    top: 25%;
    left: 25%;
    width: 24rem; /* 96 */
    height: 24rem;
    background-color: color-mix(in srgb, var(--color-primary) 10%, transparent);
  }

  .blob-2 {
    bottom: 25%;
    right: 25%;
    width: 500px;
    height: 500px;
    background-color: color-mix(in srgb, #66B9D2 10%, transparent);
    animation-delay: 1s;
    filter: blur(150px);
  }

  /* Content */
  .hero-content {
    position: relative;
    z-index: 10;
    width: 100%;
    padding: 0 1rem;
    text-align: center;
  }

  .content-wrapper {
    max-width: 48rem; /* 3xl */
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  /* Badge */
  .badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    border-radius: 9999px; /* full */
    background-color: color-mix(in srgb, var(--color-primary) 5%, transparent);
    border: 1px solid color-mix(in srgb, var(--color-primary) 10%, transparent);
    color: var(--color-primary);
    font-size: 0.875rem; /* sm */
    font-weight: 700;
    margin-bottom: 2rem;
    line-height: 1;
  }

  .ping-wrapper {
    position: relative;
    display: flex;
    width: 0.5rem;
    height: 0.5rem;
  }

  .ping-dot {
    position: absolute;
    display: inline-flex;
    height: 100%;
    width: 100%;
    border-radius: 50%;
    background-color: var(--color-primary);
    opacity: 0.75;
    animation: ping 1s cubic-bezier(0, 0, 0.2, 1) infinite;
  }

  .dot {
    position: relative;
    display: inline-flex;
    border-radius: 50%;
    height: 0.5rem;
    width: 0.5rem;
    background-color: var(--color-primary);
  }

  /* Headline */
  .headline {
    font-size: 3rem; /* 5xl */
    font-weight: 900;
    letter-spacing: -0.05em; /* tight */
    line-height: 1.1;
    margin-bottom: 2rem;
  }

  .sub-headline {
    display: block;
    margin-top: 0.5rem;
    font-size: 2.25rem; /* 4xl */
    color: color-mix(in srgb, var(--color-base-content) 90%, transparent);
  }

  .gradient-text {
    background: linear-gradient(to right, var(--color-primary), #66B9D2, var(--color-primary));
    background-size: 200% auto;
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    animation: gradient 8s linear infinite;
  }

  /* Description */
  .description {
    font-size: 1.125rem; /* lg */
    color: color-mix(in srgb, var(--color-base-content) 60%, transparent);
    line-height: 1.625;
    margin-bottom: 3rem;
    max-width: 42rem; /* 2xl */
    margin-left: auto;
    margin-right: auto;
    font-weight: 500;
  }

  /* CTA Buttons */
  .cta-group {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    height: 4rem; /* h-16 */
    padding: 0 2.5rem; /* px-10 */
    border-radius: 1rem; /* rounded-2xl */
    font-weight: 800; /* black/bold */
    text-decoration: none;
    transition: all 0.2s;
    border: none;
    cursor: pointer;
    gap: 0.75rem;
  }

  .btn-primary {
    background-color: var(--color-primary);
    color: var(--color-primary-content);
    box-shadow: 0 20px 25px -5px color-mix(in srgb, var(--color-primary) 20%, transparent);
  }
  .btn-primary:hover {
    box-shadow: 0 25px 30px -5px color-mix(in srgb, var(--color-primary) 40%, transparent);
  }

  .btn-ghost {
    background-color: transparent;
    color: inherit;
  }
  .btn-ghost:hover {
    background-color: var(--color-base-200);
  }

  .icon { width: 1.5rem; height: 1.5rem; }
  .icon-sm { width: 1.25rem; height: 1.25rem; }

  /* Bottom Decoration */
  .bottom-decoration {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 1px;
    background: linear-gradient(to right, transparent, color-mix(in srgb, var(--color-base-content) 10%, transparent), transparent);
  }

  /* Responsive Breakpoints */
  .hide-mobile { display: none; }

  @media (min-width: 640px) { /* sm */
    .hide-mobile { display: block; }
    .cta-group { flex-direction: row; }
  }

  @media (min-width: 1024px) { /* lg */
    .headline { font-size: 4.5rem; /* 7xl */ }
    .sub-headline { font-size: 3.75rem; /* 6xl */ }
    .description { font-size: 1.25rem; /* xl */ }
  }

  /* Animations */
  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: .5; }
  }

  @keyframes ping {
    75%, 100% { transform: scale(2); opacity: 0; }
  }

  @keyframes gradient {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }
</style>
