---
const techStacks = [
  {
    category: "Framework",
    title: "Flutter",
    description: "美しく、高速で、直感的なUI。すべてのOSへの対応を一本化し、読書効率を最大化します。"
  },
  {
    category: "State Management",
    title: "Riverpod",
    description: "堅牢な状態管理とリアクティブな設計。データの流れを透明化し、バグの少ない安定性を実現。"
  },
  {
    category: "Database",
    title: "Drift",
    description: "型安全なSQLite。ページ履歴やダウンロードした小説を高速かつ確実に管理します。"
  },
  {
    category: "Internal Tool",
    title: "narou_parser",
    description: "「小説家になろう」のために独自開発。高速なHTML解析と正確なデータ変換を担うエンジン。"
  },
  {
    category: "Internal Tool",
    title: "tategaki",
    description: "小説の「読みやすさ」に妥協しない。Web技術では難解な縦書き表示を Flutter で実現した独自ライブラリ。"
  },
  {
    category: "Internal Tool",
    title: "riverpod_swr",
    description: "SWRパターンを Riverpod に統合。高速なキャッシュ表示と最新データの取得をシームレスに。"
  }
];
---

<section id="tech" class="section">
  <!-- Soft ambiance backgrounds -->
  <div class="bg-gradient"></div>

  <div class="container">
    <div class="header">
      <h2 class="sub-title">Technical Craftsmanship</h2>
      <p class="title">信頼を支える技術</p>
      <div class="divider"></div>
    </div>

    <div class="grid">
      {techStacks.map(tech => (
        <div class="card group">
          <div class="mb-6">
            <span class="badge">
              {tech.category}
            </span>
            <h3 class="card-title">{tech.title}</h3>
          </div>
          
          <p class="card-description">
            {tech.description}
          </p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .section {
    position: relative;
    padding: 6rem 0; /* py-24 */
    background-color: var(--color-base-100);
    overflow: hidden;
  }

  .bg-gradient {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at 20% 30%, color-mix(in srgb, var(--color-primary) 5%, transparent) 0%, transparent 50%);
    pointer-events: none;
  }

  .header {
    text-align: center;
    margin-bottom: 4rem; /* mb-16 */
    position: relative;
    z-index: 10;
  }

  .sub-title {
    font-size: 0.875rem; /* text-sm */
    font-weight: 700;
    color: var(--color-primary);
    letter-spacing: 0.1em; /* tracking-widest */
    text-transform: uppercase;
    margin-bottom: 0.75rem;
  }

  .title {
    font-size: 2.25rem; /* 4xl */
    font-weight: 900;
    letter-spacing: -0.05em; /* tight */
    margin: 0 0 1rem;
  }

  .divider {
    height: 0.375rem; /* h-1.5 */
    width: 5rem; /* w-20 */
    background-color: var(--color-primary);
    margin: 0 auto;
    border-radius: 9999px;
  }

  /* Grid */
  .grid {
    display: grid;
    gap: 2rem;
    position: relative;
    z-index: 10;
  }

  /* Card */
  .card {
    padding: 2rem; /* p-8 */
    background-color: color-mix(in srgb, var(--color-base-200) 40%, transparent);
    border: 1px solid color-mix(in srgb, var(--color-base-content) 5%, transparent);
    border-radius: 1.5rem; /* rounded-3xl */
    backdrop-filter: blur(4px);
    transition: all 0.3s;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    text-align: left;
  }

  .card:hover {
    background-color: color-mix(in srgb, var(--color-base-200) 80%, transparent);
    border-color: color-mix(in srgb, var(--color-primary) 20%, transparent);
  }

  .mb-6 { margin-bottom: 1.5rem; }

  /* Badge */
  .badge {
    display: inline-block;
    padding: 0.25rem 0.75rem; /* px-3 py-1 */
    font-size: 0.625rem; /* text-[10px] */
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em; /* tracking-wider */
    color: var(--color-primary);
    background-color: color-mix(in srgb, var(--color-primary) 10%, transparent);
    border-radius: 9999px;
    margin-bottom: 0.75rem;
  }

  .card-title {
    font-size: 1.875rem; /* 3xl */
    font-weight: 900;
    transition: color 0.2s;
    margin: 0;
  }

  .card:hover .card-title {
    color: var(--color-primary);
  }

  .card-description {
    color: color-mix(in srgb, var(--color-base-content) 60%, transparent);
    line-height: 1.625;
    font-size: 0.875rem; /* text-sm */
  }

  /* Responsive */
  @media (min-width: 768px) { /* md */
    .grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) { /* lg */
    .section {
      padding: 8rem 0; /* py-32 */
    }
    .header {
      margin-bottom: 6rem; /* mb-24 */
    }
    .title {
      font-size: 3rem; /* 5xl */
    }
    .grid {
      grid-template-columns: repeat(3, 1fr);
    }
    .card-description {
      font-size: 1rem; /* text-base */
    }
  }
</style>
